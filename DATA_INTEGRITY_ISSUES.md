# יומן תקלות ולקחים — מונדיאל 2026 → קלט לתכנון האפליקציה

> מטרה: לתעד את **כל** תקלות שלמות-הנתונים והניקוד שהתגלו בפרויקט הדשבורד (מבוסס Excel + data.json),
> כדי שהאפליקציה החדשה (ליגת האלופות 2026/27) תמנע כל אחת מהן **בעיצוב**.
> כל התקלות חולקות שורש אחד: **המסע דרך Excel** (מנהל שולח קובץ → משתתפים ממלאים → מוחזר → מיובא ל-data.json).

---

## שורש הבעיה: מחזור ה-Excel

הזרימה הנוכחית: מנהל → workbook ב-Excel → מילוי ידני ע"י המשתתף → החזרה → **ייבוא** ל-`data.json` → ניקוד → דשבורד. כל שלב מוסיף סיכון: פרשנות-אוטומטית של Excel, עמודות עזר, ייבוא שבוחר עמודה שגויה, נוסחאות ניקוד שבירות, וכתיבה מקבילה מכמה מקורות.

---

## קטלוג התקלות (עם דוגמאות אמיתיות)

### A. פרשנות-תאריך אוטומטית של Excel
ניחוש תוצאה/טווח כמו `"7-8"` או `"2-2"` מתפרש ע"י Excel כ**תאריך** (8 ביולי → `2026-07-08`). בייבוא נקלטה גרסת התאריך במקום הטקסט הנקי.
- **מופעים:** יואב יוקלסון Q14 (`7-8`→`2026-07-08`), ענת+עמית עובדיה Q14 (`5-6`), מירית אננבורג Q86/Q87/Q88, יוסי אדרי Q87.
- **מיקום:** בדשבורד (הייבוא) — בקובץ הניחוש עצמו הערך היה נקי בעמודה נפרדת.

### B. שבירוּת פענוח שאלות דו-רכיביות (bucket + team)
שאלות "מלך שערים/פנדלים/הנבחרת עם הכי הרבה" (מזהים 75,76,77,78,79 ועוד) מנוקדות 10+10 (שם + מספר/בקט). הפורמט נשמר לא-עקבי:
- **בקט קטוע:** `"...: 4"` במקום `"3-4"`, `"...: 16"` במקום `"16-20"` (תומר עמבר Q78/Q79).
- **פורמט מקוצר:** `"פורטוגל + 3-4"` במקום המחרוזת המלאה (ליעד קרמר Q77/78/79).
- **מספרים שהוסרו לגמרי:** ניחושי מספר-השערים לשאלות 75/76 הוסרו בתהליך קודם ושוחזרו מ-`updated_predictions_audit.csv`.
- **תוצאה:** רכיב המספר (10 נק') לא נספר → תת-ניקוד.

### C. שגיאות נוסחה בצד הקובץ (text vs number)
נוסחת ההשוואה בקובץ (`IF(pred=answer,10,0)`) מחזירה 0 כשהניחוש נשמר כ**טקסט** והתשובה כ**מספר** (או להפך), גם כשהניחוש **נכון**.
- **מופע:** רועי הדר Q67 — ניחש `2`, התשובה `2`, אך הקובץ נתן 0. **הדשבורד צדק (10); הקובץ שגה.**

### D. ייבוא שבוחר עמודה שגויה
כשבקובץ יש גם עמודת ערך-נקי וגם עמודת-עזר (למשל תאריך מפורשׂ), הייבוא קלט את העמודה הלא-נכונה.
- **תוצאה:** ראו A — הדשבורד קיבל את התאריך במקום הטקסט.

### E. הבדל שיטת דירוג (תצוגה)
הדשבורד משתמש ב**דירוג צפוף** (dense: 1,2,2,3); אפליקציות טיפוסיות (hamishak) ב**דירוג תחרותי** (1,2,2,4). הטוטלים זהים — רק מספרי המקומות שונים אחרי כל שוויון (232/242 שונים). **לא באג — הבדל קונבנציה.**

### F. התנגשות כותבים מקבילים
תהליך "גביע יוסי" נפרד דחף ל-`main` במקביל, לעיתים בסדר משתתפים שונה → diff ענק והתנגשויות. נדרש `fetch`+שימור-סדר לפני כל דחיפה.

---

## טבלת מופעים מרוכזת (סיכום ההשוואה מול הקובץ הסופי)

| משתתף | שאלה | סוג תקלה | היכן | השפעת ניקוד | סטטוס |
|--------|:---:|:---:|:---:|:---:|:---:|
| ליעד קרמר | 77,78,79 | B (בקט/פורמט) | דשבורד | −30 (תת-ניקוד) | תוקן ✅ |
| תומר עמבר | 78,79 | B (בקט קטוע) | דשבורד | −20 | תוקן ✅ |
| מירית אננבורג | 86,87 | A (תאריך) | דשבורד | −20 | תוקן ✅ |
| יוסי אדרי | 87 | A (תאריך) | דשבורד | −10 | תוקן ✅ |
| יואב יוקלסון | 14 | A+D (תאריך) | דשבורד | 0 (קוסמטי) | נוקה ✅ |
| ענת+עמית עובדיה | 14 | A+D (תאריך) | דשבורד | 0 (קוסמטי) | נוקה ✅ |
| מירית אננבורג | 88 | A (תאריך) | דשבורד | 0 (קוסמטי) | נוקה ✅ |
| **רועי הדר** | 67 | C (text/number) | **הקובץ** | הקובץ שגה ב-−10 | **דווח למנהל לתיקון בקובץ** |

**סיכום:** 237/242 תאמו מראש; לאחר התיקונים — 241/242, ונותר רק רועי הדר (שם **הקובץ** טעה, לא הדשבורד).

---

## דרישות לאפליקציה החדשה — כיצד כל תקלה נמנעת בעיצוב

| תקלה | מנגנון מונע באפליקציה |
|------|----------------------|
| **A. תאריכי-Excel** | אין Excel כלל. קלט ניחוש דרך **רכיבי UI מוקלדים** (ScorePicker, בורר בקט) → הערך נשמר טיפוסית ב-DB (`int`/`enum`/`text` מוגדר). אי-אפשר "להפוך לתאריך". |
| **B. פענוח דו-רכיבי** | שאלה דו-רכיבית = **שני שדות נפרדים** (team + bucket) עם `options` מוגדרים, לא מחרוזת אחת לפרסור. הניקוד 10+10 על שדות מובנים. |
| **C. text/number בניקוד** | הניקוד רץ ב**מנוע דטרמיניסטי** (TypeScript טהור) עם טיפוסים; השוואה מספרית מול מספרית תמיד. אין נוסחאות Excel. |
| **D. עמודה שגויה בייבוא** | אין ייבוא. הניחוש הוא **מקור-אמת יחיד** ב-DB, נכתב ישירות ע"י המשתתף. |
| **E. שיטת דירוג** | `ranking_mode` ברמת ה-Season (ברירת מחדל **competition**, ניתן לשינוי). מוגדר מפורשות, לא משתמע. |
| **F. כותבים מקבילים** | כותב יחיד (Edge Function `score-season`) עם transaction; RLS מונע כתיבה מקבילה מגורמים אחרים. |
| **כללי — נכונות** | **audit invariant** רץ בכל ריצת ניקוד וחוסם פרסום אם משהו לא מתאזן (המשך ישיר ל-`audit.py` שהציל אותנו כאן). |

---

## השלכות לבדיקות (regression)

- להשתמש ב-`data.json` הסופי (146 שאלות, טבלה סופית) כ**fixture רגרסיה** — המנוע החדש חייב לשחזר את הטוטלים הסופיים המתוקנים.
- **קורפוס פרסור דו-רכיבי:** לכלול את כל צורות המחרוזת שראינו (מלא, מקוצר, בקט קטוע, תיקו `" / "`) כמקרי-בדיקה — הן היו מקור הבאגים מספר 1.
- **מקרי-קצה בקט:** ערך בדיוק על גבול (20 → `16-20`).
- **void:** שאלה שבוטלה (131) → 0 לכולם, maxPoints 0.

---

*מסמך זה נכתב בתום הפרויקט (מונדיאל 2026) כקלט לתכנון אפליקציית ליגת האלופות 2026/27. ראו גם: `UCL_APP_BLUEPRINT.md` (תכנית הבנייה), `PROJECT_DOCUMENTATION.md` (מפרט הניקוד).*
