name: Update World Cup Dashboard

on:
  workflow_dispatch:
  schedule:
    # Runs every 20 minutes. GitHub may delay scheduled jobs slightly.
    - cron: "*/20 * * * *"

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Update results and open-question trackers
        run: python scripts/update_from_fifa.py

      - name: Commit updated dashboard files
        run: |
          git config user.name "worldcup-dashboard-bot"
          git config user.email "worldcup-dashboard-bot@users.noreply.github.com"
          git add data.json leaderboard.csv matches.csv open_questions.csv open_question_review.csv sources_all.csv sources_fifa.csv efi_katash_audit.csv automation_status.json
          git commit -m "Auto-update dashboard results and open questions" || echo "No changes to commit"
          git push
